<header>
  <div class="text-center">
    <h1>Welcome <%= @user.username %>!</h1>
  </div>
</header>
<%= link_to "Create a Viewing Party", new_event_path(api_movie_id: @movie[:id]), class: "btn btn-dark text-light" %>
<h2><%= @movie[:title] %></h2>

<section>
  <p>
    <b>Vote Average:</b> <%= @movie[:vote_average] %>
    <b class="runtime">Runtime:</b> <%= @movie[:runtime]/60 %>hr <%= @movie[:runtime] % 60 %>min
    <b class="genres">Genre(s):</b> <%= @movie[:genres].map do |genre_data| %>
                                      <% genre_data[:name] %>
                                    <% end.sort.join(", ") %>
  </p><hr>
</section>

<section class="summary">
  <p><b>Summary:</b><br><%= @movie[:overview] %></p>
</section>

<section class="casts">
  <p><b>Cast:</b></p>
  <% @casts.each do |cast| %>
    <p><%= cast.name %> as <%= cast.character %></p>
  <% end %>
</section>

<section class="reviews">
  <hr><p><b> <%= @reviews.size %> Review(s)</b></p>
  <% @reviews.each do |review| %>
    <p><b>Author:</b> <%= review.author %></p>
    <p><%= review.content %></p>
  <% end %>
</section>
